<template>
    <Head title="Combined Service History Ledger" />
    <AdminLayout title="Combined Service History Ledger" eyebrow="Service tracking">
        <ServiceHistoryTabs />
        <section class="mt-5 rounded-lg border border-slate-200 bg-white p-5 shadow-sm"><p class="text-xs font-semibold uppercase tracking-wide text-teal-700">Unified activity</p><h2 class="mt-2 text-3xl font-semibold text-slate-950">Dealer and customer service lookups in one timeline.</h2><p class="mt-2 max-w-3xl text-sm font-semibold leading-7 text-slate-600">Compare channel usage, workshop records, lookup outcomes and collected service charges.</p></section>
        <section class="mt-5 grid gap-3 sm:grid-cols-2 xl:grid-cols-5"><Metric label="Filtered searches" :value="stats.total" /><Metric label="Successful" :value="stats.successful" tone="teal" /><Metric label="Failed" :value="stats.failed" tone="red" /><Metric label="Revenue" :value="money(stats.revenue)" tone="blue" /><Metric label="Customer / Dealer" :value="`${money(stats.customer_charge)} / ${money(stats.dealer_charge)}`" tone="orange" /></section>
        <section class="mt-5 rounded-lg border border-slate-200 bg-white p-5 shadow-sm"><form class="grid gap-3 md:grid-cols-2 xl:grid-cols-[1.3fr_190px_190px_auto]" @submit.prevent="filter"><Field label="Vehicle number"><input v-model="form.search" class="admin-input uppercase" type="search" placeholder="BR01AB1234" /></Field><Field label="From"><input v-model="form.from_date" class="admin-input" type="date" /></Field><Field label="To"><input v-model="form.to_date" class="admin-input" type="date" /></Field><div class="flex items-end gap-2 md:col-span-2 xl:col-span-1"><button class="h-12 rounded-lg bg-slate-950 px-5 text-sm font-semibold text-white hover:bg-teal-700">Filter</button><Link href="/admin/service-tracking/service-history" class="grid h-12 place-items-center rounded-lg border border-slate-200 px-5 text-sm font-semibold text-slate-700 hover:bg-slate-50">Clear</Link></div></form></section>
        <section class="mt-5 overflow-hidden rounded-lg border border-slate-200 bg-white shadow-sm"><div class="overflow-x-auto"><table class="w-full min-w-[1020px] text-left text-sm"><thead class="bg-slate-50 text-xs font-semibold uppercase tracking-wide text-slate-500"><tr><th class="px-5 py-3">Channel</th><th class="px-5 py-3">User</th><th class="px-5 py-3">Vehicle</th><th class="px-5 py-3">Services</th><th class="px-5 py-3">Outcome</th><th class="px-5 py-3">Charge</th><th class="px-5 py-3">Created</th><th class="px-5 py-3 text-right">Action</th></tr></thead><tbody class="divide-y divide-slate-100">
            <tr v-for="item in searches.data" :key="`${item.type}-${item.id}`" class="hover:bg-slate-50"><td class="px-5 py-4"><Channel :type="item.type" /></td><td class="px-5 py-4"><p class="font-semibold text-slate-950">{{ item.user_name }}</p><p class="mt-1 text-xs font-semibold text-slate-500">{{ item.phone || 'Phone unavailable' }}</p></td><td class="px-5 py-4 font-semibold uppercase text-slate-950">{{ item.vehicle_number }}</td><td class="px-5 py-4 font-semibold text-slate-950">{{ item.service_count }}</td><td class="px-5 py-4"><Status :success="item.is_success" /></td><td class="px-5 py-4 font-semibold text-slate-950">{{ money(item.charge_amount) }}</td><td class="px-5 py-4 font-semibold text-slate-600">{{ item.created_at || 'N/A' }}</td><td class="px-5 py-4 text-right"><Link :href="item.show_url" class="inline-flex rounded-lg bg-slate-950 px-3 py-2 text-xs font-semibold text-white hover:bg-teal-700">View record</Link></td></tr>
            <tr v-if="!searches.data.length"><td colspan="8" class="px-5 py-14 text-center"><p class="text-lg font-semibold text-slate-950">No service activity found</p></td></tr>
        </tbody></table></div><div class="border-t border-slate-100 px-5 py-4"><PaginationLinks :links="searches.links" /></div></section>
    </AdminLayout>
</template>
<script setup lang="ts">
import {defineComponent,h,reactive} from 'vue'; import {Head,Link,router} from '@inertiajs/vue3'; import AdminLayout from '@/Layouts/AdminLayout.vue'; import PaginationLinks from '@/Components/Admin/PaginationLinks.vue'; import ServiceHistoryTabs from '@/Components/Admin/ServiceHistoryTabs.vue';
type Item={id:number;type:'dealer'|'customer';user_name:string;phone?:string|null;vehicle_number:string;service_count:number;is_success:boolean;charge_amount:number;created_at?:string|null;show_url:string}; const props=defineProps<{searches:{data:Item[];links:Array<{url:string|null;label:string;active:boolean}>};filters:{search:string;from_date:string;to_date:string};stats:{total:number;successful:number;failed:number;revenue:number;customer_charge:number;dealer_charge:number}}>();const form=reactive({...props.filters});const money=(v:number|string)=>`Rs ${new Intl.NumberFormat('en-IN',{maximumFractionDigits:2}).format(Number(v||0))}`;const filter=()=>{const p:Record<string,string>={};Object.entries(form).forEach(([k,v])=>{if(v)p[k]=v;});router.get('/admin/service-tracking/service-history',p,{preserveState:true,preserveScroll:true});};
const Field=defineComponent({props:{label:{type:String,required:true}},setup(p,{slots}){return()=>h('label',[h('span',{class:'mb-2 block text-sm font-semibold text-slate-700'},p.label),slots.default?.()]);}});const Metric=defineComponent({props:{label:{type:String,required:true},value:{type:[String,Number],required:true},tone:{type:String,default:'slate'}},setup(p){const c:any={slate:'border-slate-200 bg-white',teal:'border-teal-100 bg-teal-50',red:'border-red-100 bg-red-50',blue:'border-blue-100 bg-blue-50',orange:'border-orange-100 bg-orange-50'};return()=>h('div',{class:['rounded-lg border p-4 shadow-sm',c[p.tone]]},[h('p',{class:'text-xl font-semibold text-slate-950'},typeof p.value==='number'?new Intl.NumberFormat('en-IN').format(p.value):p.value),h('p',{class:'mt-1 text-xs font-semibold uppercase tracking-wide text-slate-500'},p.label)]);}});const Status=defineComponent({props:{success:{type:Boolean,required:true}},setup(p){return()=>h('span',{class:['inline-flex rounded-md px-2.5 py-1 text-xs font-semibold',p.success?'bg-teal-50 text-teal-700 ring-1 ring-teal-100':'bg-red-50 text-red-700 ring-1 ring-red-100']},p.success?'Successful':'Failed');}});const Channel=defineComponent({props:{type:{type:String,required:true}},setup(p){return()=>h('span',{class:['inline-flex rounded-md px-2.5 py-1 text-xs font-semibold',p.type==='dealer'?'bg-blue-50 text-blue-700 ring-1 ring-blue-100':'bg-orange-50 text-orange-700 ring-1 ring-orange-100']},p.type==='dealer'?'Dealer':'Customer');}});
</script>
